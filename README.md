# poschenrieder.io

Personal homepage / hub for Robert Poschenrieder, served via GitHub Pages at the apex domain `poschenrieder.io`.

Links out to individual projects, each hosted on its own subdomain (e.g. `luftdings.poschenrieder.io`). Plain static HTML + one stylesheet, no build step (`.nojekyll`).
